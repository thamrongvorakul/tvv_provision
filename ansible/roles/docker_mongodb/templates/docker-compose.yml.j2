version: '3'
services:
  mongo:
    image: mongo:4
    command: mongod
    restart: unless-stopped
    ports:
      - 27017:27017
    volumes:
      - ${MONGO_DATA_PATH}:/data/db
    logging:
      driver: "json-file"
      options:
        max-size: "10M"
        max-file: "1"
